Compiling: examples/imports.jvmb -> imports.jvmb.class

=== Building IR ===

Intermediate Representation:
CompilationUnit(imports.jvmb)
  Imports:
    import java.util.ArrayList
    import java.util.HashMap
    import java.util.Date
    import java.io.File
    import java.io.*
  Statements:
    $lambda$("=== ArrayList ===")
    var list: Reference[name=ArrayList] = new Reference[name=ArrayList]()
    $lambda$("First")
    $lambda$("Second")
    $lambda$("Third")
    $lambda$(("List size: " CONCAT $lambda$()))
    $lambda$(("First item: " CONCAT $lambda$()))
    $lambda$(("Contains 'Second': " CONCAT $lambda$()))
    $lambda$(("  List contents: " CONCAT $lambda$()))
    $lambda$("")
    $lambda$("=== HashMap ===")
    var map: Reference[name=HashMap] = new Reference[name=HashMap]()
    $lambda$("name", "Alice")
    $lambda$("age", 30)
    $lambda$("city", "New York")
    $lambda$(("Map size: " CONCAT $lambda$()))
    $lambda$(("Map contents: " CONCAT $lambda$()))
    $lambda$(("Contains 'name': " CONCAT $lambda$()))
    $lambda$("")
    $lambda$("=== Date ===")
    var now: Reference[name=Date] = new Reference[name=Date]()
    $lambda$(("Current time: " CONCAT $lambda$()))
    $lambda$(("Time in millis: " CONCAT $lambda$()))
    $lambda$("")
    $lambda$("=== File ===")
    var file: Reference[name=File] = new Reference[name=File](".")
    $lambda$(("Current directory: " CONCAT $lambda$()))
    $lambda$(("Exists: " CONCAT $lambda$()))
    $lambda$(("Is directory: " CONCAT $lambda$()))
    $lambda$("")
    $lambda$("=== Wildcard Import ===")
    var tempFile: Reference[name=File] = new Reference[name=File]("/tmp/test.txt")
    $lambda$(("Temp file path: " CONCAT $lambda$()))
    $lambda$("")
    $lambda$("All import tests complete!")


Parse successful - no code generation (parse-only mode)
