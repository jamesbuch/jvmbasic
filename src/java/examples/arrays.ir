Compiling: examples/arrays.jvmb -> arrays.jvmb.class

=== Building IR ===

Intermediate Representation:
CompilationUnit(arrays.jvmb)
  Functions:
    Function sumArray(arr: Array[elementType=Primitive[kind=INT]], length: Primitive[kind=INT]) -> Primitive[kind=INT]
    Function findMax(arr: Array[elementType=Primitive[kind=INT]], length: Primitive[kind=INT]) -> Primitive[kind=INT]
    Function findMin(arr: Array[elementType=Primitive[kind=INT]], length: Primitive[kind=INT]) -> Primitive[kind=INT]
  Statements:
    Console.WriteLine("=== Integer Array ===")
    var numbers: Array[elementType=Primitive[kind=INT]] = new Primitive[kind=INT][5]
    numbers[0] = 10
    numbers[1] = 20
    numbers[2] = 30
    numbers[3] = 40
    numbers[4] = 50
    Console.Write("Array contents: ")
    for i = 0 to 4 { ... }
    Console.WriteLine("")
    var sum: Primitive[kind=INT] = 0
    for i = 0 to 4 { ... }
    Console.WriteLine($"Sum: {sum}")
    Console.WriteLine("")
    Console.WriteLine("=== String Array ===")
    var names: Array[elementType=Reference[name=java.lang.String]] = new Reference[name=java.lang.String][3]
    names[0] = "Alice"
    names[1] = "Bob"
    names[2] = "Charlie"
    Console.WriteLine("Names in array:")
    for i = 0 to 2 { ... }
    Console.WriteLine("")
    Console.WriteLine("=== For Each ===")
    Console.WriteLine("Names using for-each:")
    for each name in names { ... }
    Console.WriteLine("")
    Console.WriteLine("=== Array Functions ===")
    Console.WriteLine($"Sum: {sumArray(numbers, 5)}")
    Console.WriteLine($"Max: {findMax(numbers, 5)}")
    Console.WriteLine($"Min: {findMin(numbers, 5)}")
    Console.WriteLine("")
    Console.WriteLine("=== Array Modification ===")
    Console.WriteLine("Original array:")
    for i = 0 to 4 { ... }
    Console.WriteLine("")
    for i = 0 to 4 { ... }
    Console.WriteLine("After doubling:")
    for i = 0 to 4 { ... }
    Console.WriteLine("")
    Console.WriteLine("")
    Console.WriteLine("=== Complex Array Operations ===")
    var data: Array[elementType=Primitive[kind=INT]] = new Primitive[kind=INT][10]
    for i = 0 to 9 { ... }
    var temp: Primitive[kind=INT] = data[0]
    data[0] = data[9]
    data[9] = temp
    Console.WriteLine("After swap (first and last):")
    for i = 0 to 9 { ... }
    Console.WriteLine("")
    for i = 0 to 4 { ... }
    Console.WriteLine("After reverse:")
    for i = 0 to 9 { ... }
    Console.WriteLine("")
    Console.WriteLine("")
    Console.WriteLine("=== Boolean Array ===")
    var flags: Array[elementType=Primitive[kind=BOOLEAN]] = new Primitive[kind=BOOLEAN][5]
    flags[0] = true
    flags[1] = false
    flags[2] = true
    flags[3] = true
    flags[4] = false
    var trueCount: Primitive[kind=INT] = 0
    for i = 0 to 4 { ... }
    Console.WriteLine($"True count: {trueCount} out of 5")


Parse successful - no code generation (parse-only mode)
